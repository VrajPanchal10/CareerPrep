# Production API Backend Target for Vercel Builds
# [PROXY FIX] Kept blank so the Vercel proxy (in vercel.json) can route /api requests to fix mobile cookies.
# To REVERT this fix, change it back to: VITE_API_URL=https://careerprep-sbm1.onrender.com
VITE_API_URL=
